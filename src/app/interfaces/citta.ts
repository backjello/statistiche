export interface Citta {
    admin1:string, //regione
    admin3:string, //comune
    name:string, // nome
    country:string, //stato
    country_code:string //codice ISO dello stato
    latitude:number, 
    longitude:number
}
